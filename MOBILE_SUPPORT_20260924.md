# スマホ画面対応（2026-09-24）

システム概要（システム全体構成図：CloudFront経由でPC/スマートフォンいずれもアクセス）およびシステム詳細（画面ID 01030103＝会員詳細スマホ）を踏まえ、ワイヤーフレーム一式にスマートフォン表示対応を追加。

## 見つかった問題
- 全78ファイルに `viewport` メタタグが無く、スマホ実機では等倍で表示されずPCレイアウトのまま縮小されていた。
- サイドバー（幅240px固定）が常時表示のため、スマホ幅では本文が圧迫される構造だった。
- 「01030103_member_detail_mobile.html」（会員詳細スマホ）が、ファイル名のみモバイル用で中身はPC版（01030102）と同一レイアウトのままだった。

## 対応内容（全画面共通）
1. 全ファイルに `<meta name="viewport" content="width=device-width, initial-scale=1">` を追加。
2. サイドバーをオフキャンバス化（`lg`未満で非表示・ハンバーガーボタンで開閉、`lg`以上は従来どおり常時表示）。ヘッダーにハンバーガーボタンとオーバーレイを追加。
3. ヘッダー右上の検索ボックス（固定幅 `w-72`）は `sm`未満で非表示に変更。
4. 主要コンテンツの `grid-cols-2 / 3 / 4` を、`grid-cols-1 sm:grid-cols-2` のようにスマホ幅では1カラム基準に変更。
5. `<table>` を含む一覧画面（45画面）はテーブルを `overflow-x-auto` でラップし、スマホ幅でも横スクロールで一覧を確認できるようにした。

## 01030103（会員詳細スマホ）個別対応
PC版をそのまま縮小するのではなく、スマホ管理画面として作り直し：
- 端末幅相当（max 428px）のコンテナに変更し、PC版の常時表示サイドバーは廃止。
- ヘッダーは「戻る／メニュー／タイトル／通知」の1行構成に整理。
- 会員サマリー・主要ステータス（保有ポイント等）をカード化し2カラムで表示。
- 「ユーザー情報／メッセージ／タイムライン」はタブ切替（PC版は左右2カラム表示だったものをスマホでは縦1列＋タブに変更）。
- 参加グループ一覧などの各種一覧は `<details>` によるアコーディオンに変更（スマホでの縦スクロール量を抑制）。
- フッターに「ログアウト／更新」を固定表示（親指の届く位置に主要アクションを配置）。
- PC版（01030102）のヘッダーから「📱 スマホ表示」リンクを追加し、相互に行き来できるようにした。

## 未対応・要確認
- 今回はワイヤーフレーム（構造・操作導線の確認用）としての対応であり、実装時のブレークポイントや実機での見え方の最終調整は別途必要。
- 01030103以外の画面をスマホ専用レイアウトとして個別に作り込む要否は未確認（現状はレスポンシブ対応のみ）。運用上オペレーターがスマホから使う画面が他にもあれば、対象を教えてください。
